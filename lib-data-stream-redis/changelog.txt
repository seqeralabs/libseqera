# lib-data-stream-redis changelog

2.0.0 - 11 Jul 2026
- BREAKING (SPI): MessageStream gains abstract poll/renew/ack/release plus the Lease<M>
  record. Any class IMPLEMENTING MessageStream must add them (the in-repo Redis/Local impls
  are updated). consume(streamId, consumer) is RETAINED as a default implemented over the
  triad, so callers of consume() remain source-compatible.
- BREAKING (subclass): the protected processMessage(...) signature dropped its trailing
  AtomicInteger count parameter; subclasses overriding it must update.
- BREAKING (behaviour): handlers now run asynchronously off the listener thread (on virtual
  threads / an injectable executor); delivery is at-least-once with a heartbeat lease and
  in-process re-poll, so consumers MUST be idempotent. concurrency() defaults to 1, so intake
  stays effectively serial until a subclass opts in to parallelism.
- Async, non-blocking consumer processing: the listener thread becomes a dispatcher that
  hands each message to a virtual-thread executor and never runs a handler itself, removing
  head-of-line blocking and enabling concurrent processing. Handlers run on a shared
  virtual-thread executor by default; supply the Micronaut @Named(BLOCKING) executor via
  AbstractMessageStream.withHandlerExecutor(...). Concurrency is bounded by a semaphore
  (see concurrency()), not by the executor.
- Worker-scoped heartbeat lease: while a handler is in-flight, a daemon renews its Redis
  entry (XCLAIM ... JUSTID) so an alive consumer is never reclaimed regardless of duration;
  the lease lapses on crash to preserve real failover. Decouples processing time from
  claim-timeout.
- Model B (hold-lease + scheduled re-poll): a not-yet-terminal command keeps its lease and
  is re-invoked in-process after pollInterval instead of waiting for a Redis reclaim,
  decoupling re-poll cadence from claim-timeout.
- SPI evolution on MessageStream: add lease triad poll/renew/ack/release plus Lease<M>
  record; add heartbeatInterval()/maxProcessingTime() (default null; overridden by the Redis
  impl to expose the configured values) so the base derives the renew cadence from the
  underlying stream's claim-timeout. consume(streamId, consumer) is retained as a default
  implemented over the triad (backward compatible, additive).
- RedisStreamConfig gains getHeartbeatInterval() (default claim-timeout/3) and
  getMaxProcessingTime() (default 15m) with their *Millis() variants; RedisMessageStream
  wires them through so the heartbeat cadence tracks any configured claim-timeout.
- AbstractMessageStream.concurrency() defaults to 1 (a semaphore ceiling on in-flight
  commands), overridable by subclasses.

1.5.0 - 14 May 2026
- BREAKING (observability): rename Outcome.FAILED -> Outcome.ACTIVE; metric tag value
  outcome="failed" is now outcome="active". The Java API is source-compatible, but any
  downstream dashboard or alert filtering on outcome="failed" will silently return no
  data after upgrade and must be updated. Real failures are unchanged: outcome="errored".
- The rename clarifies that consumer-returns-false means "work still in progress, keep
  for redelivery" rather than a failure.

1.4.0 - 13 May 2026
- Add optional Micrometer instrumentation (backlog gauge, processed counter, processing-time Timer)
- New backward-compatible constructor: AbstractMessageStream(MessageStream, StreamMetrics)
- New public types under io.seqera.data.stream.metrics: StreamMetrics, MicrometerStreamMetrics, NoopStreamMetrics, Outcome
- micrometer-core is a compileOnly dependency; AbstractMessageStream itself never references MeterRegistry,
  so subclasses without metrics remain loadable on classpaths without micrometer-core

1.3.0 - 20 Feb 2026
- Rewrite production code from Groovy to Java
- Add SleepHelper utility to simplify sleep patterns
- Update logback dependency to 1.5.25

1.2.0 - 28 Jan 2026
- Fix: Prevent message starvation in xautoclaim round-robin

1.1.3 - 26 Nov 2024
- Bump logback version to 1.5.19

1.1.2 - 4 Nov 2024
- Remove duplicate TestRedisActivation class
- Use centralized TestRedisActivation from lib-fixtures-redis test fixtures
- Add testFixtures dependency on lib-fixtures-redis

1.1.1 - 9 Sep 2024
- Fix: Create consumer group if it does not exist when retrieving messages from Redis (COMP-428)
- Improve error handling in RedisMessageStream

1.1.0 - 31 Jul 2024
- Add RedisStreamConfig interface for better configuration management
- Extract hardcoded values from RedisMessageStream into configuration
- Enhance JavaDoc documentation for LocalMessageStream and RedisMessageStream
- Add TestConfig implementation for test environment
- Improve code maintainability and configurability

1.0.0 - 26 Jul 2024
- Initial release of lib-data-stream-redis extracted from Wave project
- Implement MessageStream interface for distributed message streaming
- Add AbstractMessageStream base class with consumer management
- Add LocalMessageStream for in-memory message streaming
- Add RedisMessageStream implementation using Jedis client
- Support MessageConsumer interface for message processing
- Add consumer group management with automatic group creation
- Support message acknowledgment and pending message handling
- Use RedisActivator marker interface for conditional bean loading
- Include comprehensive test suite with Redis testcontainers
